//! ESP32 implementations of `astrameter-platform` traits.
//!
//! Real impls land in Phase 8. On non-espidf targets this crate is an empty
//! shim so that workspace tooling still loads it.

#![forbid(unsafe_code)]
